from typing import List

# class Solution:
#     def minTimeToVisitAllPoints(self, points: List[List[int]]) -> int:
#         lastPointVisited = points[0]
#         for i in points[1:]:

i = [1, 2, 3, 4, 5, 6, 7]
print(i[0:])
