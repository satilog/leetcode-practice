from typing import List


class Solution:
    def spiralOrder(self, nums: List[List[int]]):
        bottom = len(nums)
        right = len(nums[0])
        top = 0
        left = 0

        while n < max_n and m < max_m:

            print(m, n)
